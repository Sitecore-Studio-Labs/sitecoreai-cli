---
"@sitecoreai-labs/sitecoreai-cli": patch
---

The `<page>/Data` folder created for scoped page datasources now conforms to SXA's Page Data template (`/sitecore/templates/Foundation/Experience Accelerator/Local Datasources/Page Data`, resolved by path) instead of the generic Common Folder — so SXA and Pages recognise it as page-local datasource storage.
