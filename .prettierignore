dist
node_modules
coverage
*.tgz

# Generated by `pnpm docs:commands` — the generator owns the exact
# formatting, including Markdown escaping. Prettier would strip the
# backslash escapes and drift the file out of sync with the generator.
docs/commands.md

# Generated by Changesets on every release (`changeset version`). The
# content comes from the per-change `.changeset/*.md` entries; Prettier
# reformatting it here would just be re-undone by the next release,
# failing `format:check` each time.
CHANGELOG.md
